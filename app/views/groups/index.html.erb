<p style="color: green"><%= notice %></p>

<div class="group-title">
  <h1>Groups</h1>
</div>

<div id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
    <p>
      <%= link_to "Show this group", group %>
    </p>
  <% end %>
</div>

 <div class="new-group"><%= link_to "New group", new_group_path %></div>
